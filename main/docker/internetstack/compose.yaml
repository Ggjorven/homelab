networks:
  internalnetwork:
    external: true

  externalnetwork:
    external: true

services:
  # Cloudflared
  # Nginx
  # Fail2ban
  # ...
  
  # Note: This file will use the .240-.249 range for internalnetwork ip-addresses
