# =============================================================================
# NETWORK INTERFACES
# =============================================================================
define WAN_IF   = vmbr0        	# Physical uplink (bridge)
define VLAN_IF  = vmbr1         # Internal VLAN-aware bridge

# =============================================================================
# LAN SUBNET
# =============================================================================
define NET_LAN       = 192.168.0.0/22

# =============================================================================
# VLAN SUBNETS
# =============================================================================
define NET_STORAGE   = 172.20.100.0/24
define NET_MEDIA     = 172.20.101.0/24
define NET_ARR       = 172.20.105.0/24
define NET_PUBNET    = 172.20.112.0/24
define NET_PRIVNET   = 172.20.113.0/24

# All internal VLANs as a group (for NAT masquerade)
define NET_ALL_INTERNAL = 172.20.0.0/16

# =============================================================================
# HOST GATEWAY IPs (Proxmox host itself, one per VLAN)
# =============================================================================
define GW_STORAGE   = 172.20.100.1
define GW_MEDIA     = 172.20.101.1
define GW_ARR       = 172.20.105.1
define GW_PUBNET    = 172.20.112.1
define GW_PRIVNET   = 172.20.113.1

# =============================================================================
# CONTAINER IPs  (hostname -> IP for readability in rules below)
# =============================================================================
define IP_STORAGE    = 172.20.100.10
define IP_MEDIA      = 172.20.101.10
define IP_ARR        = 172.20.105.10
define IP_PUBNET     = 172.20.112.10
define IP_PRIVNET    = 172.20.113.10

# =============================================================================
# PORT GROUPS
# =============================================================================
# Proxmox
define PORT_PMX_WEBUI= 8006 

# SSH
define PORT_SSH      = 22

# Web
define PORTS_HTTP    = { 80, 443 }

# SSH
define PORT_SSH      = 22

# Jellyfin
define PORT_JELLYFIN = 8096

# Notification push
define PORT_GOTIFY   = 8008
