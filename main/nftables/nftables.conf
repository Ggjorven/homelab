table inet filter {
	chain forward {
		type filter hook forward priority 0; policy accept;
		# inter-VLAN: drop later when you're ready
	}
}

table ip nat {
	chain postrouting {
		type nat hook postrouting priority 100;
		ip saddr 172.20.0.0/16 oifname "vmbr0" masquerade
	}
}
